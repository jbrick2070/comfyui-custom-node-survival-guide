# OTR Bug Scrape - Bible Coverage Audit (2026-08-07)

Method: 5 Opus scrapers over OTR bug records (BUG_LOG.md, BUG_LOG_2026-06.md, docs/handoffs,
QA/kibitz runs, loose test logs, git fix history) -> coverage check against the 228-entry
BUG_BIBLE.yaml at 13dbdac -> adversarial verify of every flagged candidate (36 Opus batches,
verifiers instructed to default to 'covered' when a plausible match exists).

Stats: 369 bug records scraped | 156 covered at scrape |
177 resolved to existing ids on verify | 35 truly uncovered | 0 dropped.

## 35 uncovered bugs (candidates for new bible entries)

### 1. Default music engine was swapped to Stable Audio 3 while the prompt builder stayed tuned for musicgen — conflict-free default != better-sounding
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG_2026-06.md L185-198 (BUG-LOCAL-408)
- **Evidence:** the prompting or inputs for the default music is pretty bad, doesn't sound like music at all
- **Why no existing entry covers it:** I looked hard and could not find an entry that teaches this. The closest three each miss the lesson. 11.14 comes nearest -- its cause names 'a generic words-per-minute constant ... that does not match the actual TTS engine's pace' and its fix is 'measure your TTS engine's actual wpm and bake that constant in' -- i.e. engine-specific tuning must be re-derived per engine. But 11.14 is about a numeric unit-conversion constant with a countable target, and its whole repair (translate minutes to words, enforce >=70%) is inapplicable to a generative prompt. 07.04 has the same surface signature (audio model loads clean, output is garbage, verify literally reads 'melody-conditioned music actually follows the melody') but its cause is a wrong transformers class and its fix is 'read the model card, verify class matches use case' -- nothing about a prompt tuned for the previous model. 09.06 teaches 'a pattern that works for one sibling node fails for another -- confirm each node's actual expected shape' but scopes that to a hard TypeError on a parameter's type shape, not a silent semantic mismatch. What is genuinely untaught: (a) a prompt/conditioning bundle is a MODEL-SPECIFIC ASSET that does not migrate when the engine behind a slot changes -- here `_otr_music_prompt.py` was provably unchanged since the good renders (last commit ad0ac50) while `_otr_engine_profiles.py` flipped the default from musicgen to SA3, so the regression was 100% engine-swap-with-stale-prompt; (b) SA3 additionally needed inputs the old contract had no concept of (`seconds_total` as a structural context window with per-cue `seconds_start`, a real negative prompt where '' was passed); (c) the decision rule that produced it -- SA3 was picked as default because it was 'the only conflict-free new music default (no extra pip dep)', and integration convenience is not a quality criterion; (d) the failure is only detectable by a human A/B listen, and the one automated gate that exists (`test_audio_byte_identical`) actively fights the fix by design, requiring an operator-gated golden re-baseline. None of that is in the bible.

### 2. Title decode/reveal animation's completion point was pinned to the END of its window, so the fully-resolved title was on screen for only 1-2 frames
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG_2026-06.md L176-183 (BUG-LOCAL-409)
- **Evidence:** the #1 hero title card does its cool decode/scramble animation for essentially the entire opening-music duration; the FULLY-RESOLVED title only appears in the last couple seconds
- **Why no existing entry covers it:** No entry covers this. I searched the full bible for animation|reveal|scramble|fade-in|linger|hold|progress|easing|interpolat|frac|linear and found nothing in this class -- the only 'progress' hits are ComfyUI's ProgressBar/interrupt (05.04, 08.05) and `F.interpolate` resampling aliasing (07.08). The nearest timing entries all miss: 12.64 shares the title-card tag but is about zero-vs-None sentinel truthiness in an onset value (presence, not pacing); 12.69 is about a positioned timeline's total frame budget and crossfade overlap ownership at the mux boundary, not the internal pacing of one card; 12.25 is about a hardcoded `min(val, small_cap)` truncating a duration, the opposite of a span stretched too wide; 07.13 is the closest in spirit (payload lands at the wrong point inside its own window because the model burns 3-6 frames of motion-onset freeze) but its cause is a model-intrinsic latency and its fix is a silence pad plus symmetric trim -- nothing about an author-chosen animation span. The untaught class is clean and general: when an animation's progress is computed as `p = (frame - start) / (end - start)`, the payload only reaches its resolved state on the final frame, so the thing the animation exists to reveal is never actually READ. Dwell time must be designed -- the reveal span should be a bounded FRACTION of the window (BUG-409 shipped `_title_reveal_progress` with `reveal_frac` default 0.4, env `OTR_TITLE_REVEAL_FRACTION`, then clamps p to 1.0 and HOLDS) -- and the regression must assert both ends: fully resolved at `w0 + reveal_span` AND still solid at `me - 2`. This also generalizes past title cards to any scrub, wipe, typewriter, or counter animation, and it is invisible to every test the bible currently recommends because the frames all render and the durations all match.

### 3. Image-pipeline rewrite silently dropped load-bearing look inputs — the FluxGuidance node, the cinematic style suffix, the radio broadcast-distress suffix, and the deterministic bookend seed
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG_2026-06.md L139-158 (BUG-LOCAL-411)
- **Evidence:** NEW `flux_gen1.py` has **NO FluxGuidance node at all** (grep "guidance" -> nothing). **This is the biggest factor.**
- **Why no existing entry covers it:** Not covered. 12.03 is the nearest neighbour and points the WRONG WAY: it is an 8-step checklist for a half-removed dependency, i.e. the danger of references LEFT BEHIND after a swap. BUG-411 is the inverse failure -- inputs that vanished and left no reference at all, so every grep, AST parse, dead-reference scan and cross-import check in 12.02/12.03 stays green precisely because nothing dangles. 12.02's step 8 'Widget audit (INPUT_TYPES vs widgets_values)' checks count/order within a node, not semantic parity against a DELETED node. 12.42 is about intent documented in a roadmap but not enforced by tests, which is adjacent, but its subject is write-path drift with a greppable old string; here there is no old string to grep because `_otr_image_engines/flux_gen1.py`, `otr_meta_brief_image_prompt.py` and `otr_image_gen_dispatcher.py` are pure insertions and the old `visual/batch_flux_render.py` is gone. 11.38 has the right shape at the sentence level ('the compact replacement repeated field names but lost nested enum literals') but is scoped entirely to LLM structured-output schema seams. The untaught class: when a generation subsystem is REWRITTEN rather than edited, the model/steps/cfg/sampler will match (they are the obvious knobs) while the quality-BEARING inputs silently disappear -- a graph node that was never a workflow widget (`FluxGuidance` 3.5, `grep guidance` returns nothing in the new engine, self-identified as 'the biggest factor'), prompt suffixes that lived as string constants (`_DEFAULT_STYLE_SUFFIX`, `_RADIO_PROMPT_SUFFIX`), and a fixed seed (4242). Two things make it especially teachable and absent from the bible: the old configuration's only surviving record was the SAVED `widgets_values` in a historical commit of the workflow JSON (`e4cb3ac:workflows/otr_scifi_16gb_full.json`), which is a forensic source no bible entry tells you to consult; and the output kept rendering successfully, so nothing went red -- detection required A/B against a preserved reference artifact (`stills/radio_bookend_*.png`), since OTR strips PNG metadata and left the code/ledger as the only source of truth.

### 4. A cleanbreak deleted the node that held the entire tuned LTX motion recipe; the replacement engine shipped different code defaults, so the look regressed with no workflow-JSON change to point at
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG_2026-06.md L108-137 (BUG-LOCAL-412)
- **Evidence:** The motion recipe was NOT in the JSON — sampler / steps / cfg / strength / engine / loop / dims were all CODE defaults in `batch_ltx_render.py`.
- **Why no existing entry covers it:** 12.03 (the scraper's guess) is a HALF-REMOVED-DEPENDENCY checklist: find leftover imports/widgets/README refs after a library swap. It is about dangling references, not about behavioural parity of the replacement's defaults. The nearest real neighbour is 12.05 (same parameter has different defaults at parser/prompt/renderer layers; fix = grep the value across the codebase) but that is one live parameter drifting across coexisting layers, not a DELETED implementation whose defaults WERE the tuning. Nothing in the bible teaches: before a cleanbreak, capture the doomed component's effective code defaults as a golden recipe and parity-test the replacement, because the workflow JSON is unchanged and therefore useless as a diff target. Confirming signal: the sibling defect BUG-LOCAL-411 (flux image-stack rewrite silently dropped FluxGuidance + the cinematic/radio style suffixes, same 'engine rewrite regresses the LOOK' family, also flagged 'Bible candidate: yes') has no entry either -- grep for flux/guidance/style-suffix/aesthetic across the YAML returns nothing. The whole 'rewrite silently drops load-bearing look levers' family is absent.

### 5. LTX-2B was driven at the deliverable canvas 1472x832, far above its 480p native resolution, so the i2v anchor held frame 0 sharp and later frames re-noised into mush
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG_2026-06.md L109 (BUG-LOCAL-412 blur root cause)
- **Evidence:** LTX-2B re-noises into MUSH above its native res (the engine's own note: "0.75 re-noises into mush at 1472x832")
- **Why no existing entry covers it:** I pushed hard on two candidates and neither covers it. 12.73's cause-channel (3) literally names the shape -- 'the bound silently depends on a SECOND dimension (canvas, resolution, device, precision) that is itself unpinned, so the declaration is true only under conditions nothing enforces' -- which describes the i2v-strength-0.75-is-mush-at-1472x832 coupling well. But 12.73 is a capability-DECLARATION contract entry: its symptom is a late, expensive HARD failure from planner/executor disagreement, and its fix/verify are about resolvers, typed refusals for env conflicts, and menu-member testing. Nothing there is a quality-envelope lesson. 07.04 ('model loads without error but ignores conditioning and produces garbage'; fix = read the model card) shares only the generic 'runs clean, output is wrong' shape; its mechanism is a wrong transformers class from the same checkpoint. Every phase-07 video/image entry in the bible is VRAM (07.16, 07.17, 07.22), wiring/blend (07.11), or format -- there is no entry on a generative model's NATIVE RESOLUTION ENVELOPE, on 'render at native res and upscale to the deliverable canvas', or on the specific trap here: a canvas change made correctly for a sibling asset family (flux stills) silently dragged the video engine out of envelope, and the degradation is progressive WITHIN a clip (i2v anchor holds frame 0, later frames drift) rather than an error.

### 6. A bare `_core = locals()` kwargs capture swept up non-parameter locals added later by an unrelated edit, so the writer's refine loop raised TypeError on every refine-enabled run
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG_2026-06.md L75-81 (BUG-LOCAL-416)
- **Evidence:** TypeError: run() got an unexpected keyword argument 'os'
- **Why no existing entry covers it:** 05.10 is the closest entry and shares the exact trigger -- a function-body `import os` added later by an unrelated edit -- but it teaches a different mechanism and a different fix: a local `import X` makes X a LOCAL name for the whole function, so an earlier reference raises UnboundLocalError; fix = move or delete the local import. Applying 05.10's fix as written (move the import to the top of the function) would NOT prevent this bug -- `os` still lands in locals() and still gets forwarded as a kwarg. The actual lesson (never build a kwargs bundle from a bare locals() snapshot; filter it against inspect.signature(...).parameters, because any later local silently poisons the re-entry) has no entry. 12.22 matches the literal error text 'got an unexpected keyword argument' but is a bitsandbytes/Params4bit library signature mismatch, unrelated. The secondary lesson (a default-Off code path the suite never drives) is only partially touched by 05.10's verify and 12.74's route-coverage mandate.

### 7. Cast name/gender/voice incoherence — names were rolled from a gender-blind pool while the gender plan was bound to slots positionally, never reconciled
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L11 (cast-coherence sprint marker)
- **Evidence:** male-coded names landed on female slots/voices and vice versa (`MALIK HIBBERT`->female, `PHYLLIS OKAFOR`->male). The voice always followed gender correctly; only the NAME was wrong.
- **Why no existing entry covers it:** 10.06 and its xref 10.04 are the only gender-aware entries in the bible, and both are POOL-SIZING entries: symptom 'the female characters all sound the same', cause 'only 2 of 10 presets classified female', CAST_GENDER_POOL_EXHAUSTED silently reusing presets, verify 'audit per-subset count vs max demand for every filter'. This claim is the opposite situation -- supply was never the problem and, as the evidence states, the voice ALWAYS followed gender correctly; only the NAME was wrong. Running 10.06's verify against the name pool passes trivially and catches nothing. I checked the other plausible neighbours: 10.03 is an LLM typing the wrong roster name (difflib repair), 10.05 is cast-pool combinatorics/copyright/parser-compat, 12.43 is key-namespace normalisation for merge lookups, 11.32 is an unattributed announcer-row mutation, 12.61 is generic-role vs personal-identity classification. None of them teach the actual class: two correlated attributes of one entity are produced by independent generators (gender-blind name roll vs positionally-bound gender plan) and never reconciled, so every subsystem is individually correct while the artifact is internally incoherent. There is also a related unpromoted precedent in the archive (BUG-LOCAL-304, a parallel voice-preset gender descriptor table left out of sync), which confirms this family has never made it into the bible.

### 8. CI tooling wrote a multi-MB temp diff into a OneDrive-synced repo tree; OneDrive held the file open mid-sync so the next truncating re-open raised OSError(22) at OPEN time and unlink raised PermissionError
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L45 (BUG-LOCAL-293 items 4+5)
- **Evidence:** OneDrive held the multi-MB file open mid-sync so the next truncating re-open raised `OSError(22) Invalid argument` at OPEN time (before any write; even `'wb'` failed)
- **Why no existing entry covers it:** Genuinely uncovered. I searched the full 228 entries for OneDrive, Dropbox, Google Drive, cloud sync, indexer, watcher, held-open handles, retry/backoff on delete, and scratch/temp file placement — zero hits. The three nearest neighbors each fail on mechanism and remedy: (a) 12.09/12.10 do have an external Windows agent holding handles (Windows Defender locking git.exe child processes), but the symptom is a hanging git command and the fix is a Defender exclusion path — nothing about file writes, truncating reopen, or sync clients; (b) 02.14 does raise `OSError: [Errno 22] Invalid argument` on Windows, but that is errno coincidence: its cause is tqdm writing to a PIPE'd stdout the parent never drains, and its four-part fix (weights_only patch, pytorch_lightning shim, local original_config, disable progress bars) shares nothing with this; (c) 12.34 covers Windows lock state but scoped narrowly to .git/index.lock in a sandbox. Two distinct lessons are absent from the bible entirely: never write large scratch/temp artifacts inside a cloud-synced or externally-watched tree (put them in a non-synced temp dir or add an exclusion), and on Windows the failure surfaces at OPEN time on a truncating reopen — before any write, `'wb'` included — with unlink raising PermissionError, so the handling must be open-time retry/backoff plus write-new-then-rename rather than write-time error handling. Worth a new phase-12 entry.

### 9. ComfyUI 0.22.2 migrated a core loader node to the V3 node API, whose FUNCTION returns a `NodeOutput` wrapper rather than a bare tuple — a custom node that validated `isinstance(tuple|list)` crashed mid-chain
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L385-398 (BUG-LOCAL-266)
- **Evidence:** RuntimeError: OTR_HuMoTierLoader: node 'AudioEncoderLoader' returned <comfy_api.latest._io.NodeOutput object at 0x...>; expected a non-empty tuple.
- **Why no existing entry covers it:** Nothing in the bible covers a custom node invoking a CORE ComfyUI node's FUNCTION and depending on its return shape. Grepped the full 5252-line YAML for tuple / NodeOutput / comfy_api / FUNCTION / RETURN_TYPES / version / upgrade / breaking-change: the only 'tuple' hits are 06.03 (keep INPUT_TYPES values plain) and 08.03 (OUTPUT_NODE returns a plain tuple) -- both about the node's own declared surface, not about consuming another node's return. The nearest neighbours all fall short: 04.10 mentions 'upstream changes to the API' but is about /object_info optional-input ORDER for widget slot mapping; 07.15 says 'READ THE NODE SOURCE' but is about an input parameter's semantics vs model capability, not a return contract; 12.39 and 12.53 cover API/capability drift for EXTERNAL LLM providers, not the ComfyUI core the pack runs inside. The distinctive, actionable content is absent everywhere: ComfyUI is migrating core nodes to the V3 schema (`comfy_api.latest._io.NodeOutput`, positional results on `.args`, exposed via the `.result` property) one node at a time, so V1 and V3 nodes coexist in a single loader chain -- four links returned plain tuples and only AudioEncoderLoader broke, which is why the failure looks like a node-specific bug instead of an API migration. That partial-migration detail, the unwrap recipe, and the version pin (0.22.2) are exactly what phase 03/05 exists to carry, and this repo is a ComfyUI custom-node guide, so it is squarely in scope. Recommend a new entry (phase 5, execution model, or phase 3 loading) with a verify step that pins both the V1 tuple pass-through and the V3 NodeOutput unwrap, plus the empty-NodeOutput still-raises case.

### 10. Relative imports were added to a module whose documented load strategy is sys.path-prepend plus ABSOLUTE imports, so it breaks whenever loaded as a top-level module
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L593-599 (BUG-LOCAL-252), L621 (BUG-LOCAL-249 sibling)
- **Evidence:** ImportError: attempted relative import with no known parent package
- **Why no existing entry covers it:** I searched the full 5252-line bible for `relative import`, `sys.path`, `ImportError`, `absolute import`, `__package__`, and `top-level module` -- zero hits for any of them. The 26 lines that mention `import` at all cover different things: 03.01 (isolate per-node imports in try/except so one break doesn't crash siblings), 03.02 (move heavy imports into execute() for startup time), 12.23 (stale NODE_CLASS_MAPPINGS entries after a refactor -- an attribute gap, not an import-mechanics contract), 05.10 (a function-local `import os` shadowing the module-level name into UnboundLocalError -- scoping, not packaging), and 02.06 (loose version pinning around version-sensitive imports). 12.02 step 7 says 'Verify cross-imports resolve' but that is a checklist item with no stated failure class or cause. The actual lesson -- a module's import STYLE is part of its load contract, and a ComfyUI module loaded by sys.path-prepend has no parent package so any relative import is a latent break -- is not taught anywhere. The recurrence noted in the claim (BUG-LOCAL-252 with BUG-LOCAL-249 as a sibling) is evidence this class needs an entry.

### 11. Three test modules installed a PARTIAL `folder_paths` stub into `sys.modules` at import time; whichever pytest collected first won, producing 9 order-dependent failures that all passed in isolation
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L601-607 (BUG-LOCAL-251)
- **Evidence:** module 'folder_paths' has no attribute 'get_filename_list'
- **Why no existing entry covers it:** No entry covers in-process global-namespace contamination between tests, and none covers the diagnostic signature 'passes in isolation, fails in the collected suite.' The testing/harness entries are all about something else: 12.01 (extract pure functions so tests don't need a ComfyUI restart), 12.36 (exclude .venv from os.walk), 12.50 (reconcile a blank child exit code against the runner's success record), 12.35 (README/YAML/pytest three-file sync). The nearest adjacency is 12.47/12.52, and I considered them seriously before rejecting: they do teach that leftover shared state silently poisons later runs, and 12.47 even states a generic rule ('any "sourced side-file" a launcher honors must be consumed... never left to accumulate on the writer's promise to clean up'). But their mechanism is a file-based env hook leaking ACROSS separate process boots, and their fix (consume-once, delete after sourcing, log loudly) does not transfer to sys.modules ownership within one pytest process. Notably, 02.14 actually PRESCRIBES injecting a shim into sys.modules ('inject a 3-level pytorch_lightning shim into sys.modules') with no warning that a partial shim installed by one module can win the race and starve another -- so the bible currently recommends the pattern without teaching its failure mode. The missing lesson: a process-global namespace needs ONE canonical fixture owning the COMPLETE stub surface (conftest), never per-module partial stubs, and green-in-isolation is not green.

### 12. uv launcher-stub PID split: the venv python.exe fork-execs the real cpython, so os.getpid() and Popen().pid disagree — the port-owner check refused every healthy launch and the between-iter sweep killed its own supervisor
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L911-955 (BUG-LOCAL-229 cause 1)
- **Evidence:** `os.getpid()` returns the real-cpython PID, `subprocess.Popen(...).pid` returns the stub PID, and netstat reports the real cpython as the TCP socket owner
- **Why no existing entry covers it:** I looked hard and there is no entry teaching that a PID handle is not a process identity. Grepped the whole file for pid/getpid/Popen/netstat/port/socket-owner/process-tree/venv-stub/exec: the only process-identity entry is 01.04, and it teaches the opposite remedy — its cause is an Electron wrapper (ComfyUI.exe vs python.exe) and its fix is 'Kill the outermost wrapper: Stop-Process -Name ComfyUI -Force. Never target python.exe.' Applied to a uv venv stub that fork-execs and then vanishes as a distinct owner, that advice actively misleads. 12.50 is adjacent (a detached supervisor misreading a child's exit property as terminal evidence) but is about exit codes, not identity, and 02.15 covers detached-boot failures only via cp1252 encoding. Three specific lessons are absent everywhere: (a) on a uv/venv launcher stub, os.getpid(), Popen().pid, and the netstat TCP socket owner are three different values; (b) liveness/port-ownership must be resolved from the resource (socket owner or process tree), never from the handle Popen returned; (c) a PID-based between-iteration sweep built on the wrong identity can kill its own supervisor. That self-kill hazard in particular has no analogue in the bible. Worth a new phase-09 (subprocess) entry with an xref to 01.04 and 12.50.

### 13. A global `--force-fp16` launch arg upcast a natively-fp8 checkpoint to fp16 at load, doubling its footprint on a 16 GB card and forcing the offloader to page weights per sampler step
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L957-987 (BUG-LOCAL-230)
- **Evidence:** model weight dtype torch.float16, manual cast: None ... 5%|1/20 [09:24<2:58:54, 564.99s/it]
- **Why no existing entry covers it:** Searched the whole bible for fp8/fp16/precision/upcast/quantized-checkpoint/launch-arg terms. The only CLI flag any entry names is `--reserve-vram` (07.16 line 2972); nothing mentions a precision-forcing global (`--force-fp16` / `--fp16-unet` / `--force-fp32`), nothing mentions fp8/NF4/INT8 checkpoint-native dtype being overridden at load, and no entry cites the tell-tale `model weight dtype torch.float16, manual cast: None` log line. Nearest neighbours all miss the causal lesson: 07.05 is an fp32-vs-fp16 crash (mat1/mat2), not a footprint doubling; 02.07 is the `torch_dtype` deprecation rename; 06.06 is empirical model+quant+hardware incompatibility ending in a CUDA assert, fixed by dropdown filtering; 12.47 is a launcher poisoning boots via an orphaned env side-file, not an operator-set precision flag. 07.16/07.22 do cover the DOWNSTREAM symptom (no-OOM sysmem-spill crawl), but they would actively misdirect a reader here -- their prescribed fixes are 'split the dual-consumer VAE' and 'raise EXTRA_RESERVED_VRAM', and BUG_LOG.md L967 records that this exact misdiagnosis ('weights-too-big-for-card, fix the checkpoint') cost ~16 hours. The load-time-dtype-override cause is genuinely untaught.

### 14. An over-long visual prompt dilutes a small video model's motion signal — the motion verbs get buried by scene description past ~188 characters
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L1649-1657 (BUG-LOCAL-112)
- **Evidence:** LTX-Video's 2B model loses motion signal when the prompt exceeds ~161-188 characters
- **Why no existing entry covers it:** Every length/budget entry in the bible is about LLM text generation and about a HARD failure, never about a within-limits prompt silently degrading a diffusion/video model's output quality. 12.33 is an oversized prompt blowing the KV pre-fill and stalling (fix: truncate to the context cap). 11.28 is a silent n_ctx downgrade causing JSON truncation. 11.29/11.30 are output token-ceiling truncation. 12.62 is prompt+output arithmetic exceeding the context window. 11.23 is the inverse axis -- an LLM OVERSHOOTING a word floor -- and concerns generated length, not conditioning-prompt length. 07.23 carries a `prompt-authoring` tag but is about skipping image authoring when no engine consumes stills. Nothing anywhere covers 'the model's effective attention window for conditioning text is far shorter than its nominal cap, and exceeding it silently dilutes a specific semantic axis (motion) with zero error, zero truncation, and zero log signal -- so cap the composed prompt at the empirically-measured limit.' 06.06 is the closest structural analogue (empirically establish a per-model limit, then encode it as a documented constant) but its trigger is a `CUDA error: device-side assert` crash and its remedy is filtering a dropdown; it gives a reader no reason to suspect prompt length as a silent quality lever.

### 15. Colour bleed from injected era/ambient palette terms and a hardcoded lighting phrase — portraits came out blue on sci-fi episodes and the radio still came out red on period episodes
- **Source:** bug-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\BUG_LOG.md L1661-1676 (BUG-LOCAL-113)
- **Evidence:** Character portraits had a blue cast on sci-fi episodes; the radio-set still image had a red cast on period drama episodes.
- **Why no existing entry covers it:** Searched every phase-07 video/image entry, every prompt-composition entry in phase 11, and grepped the whole file for colour/palette/tint/lighting/era/prompt-token terms. Nothing teaches this class: a SHARED prompt-composition helper invoked at the wrong scope profile (era_profile='full') injecting an episode-wide colour palette into a per-subject prompt the diffusion model then takes literally, plus a hardcoded style phrase ('warm tungsten light') baked into a string builder. Nearest neighbours all fail on mechanism: 07.11 (BUG-LOCAL-092) is the only unwanted-colour-cast entry but its cause is an ffmpeg additive blend / layer ordering, and its fix is about opacity and base-layer choice — an author reading it learns nothing about prompt-token bleed into a text-conditioned image model. 04.06 covers hardcoded WIDGET defaults in workflow JSON overriding LLM output (title/filename), not hardcoded style tokens inside a prompt-builder helper. 12.05 covers the same parameter having different DEFAULTS at parser/prompt/renderer layers, not a literal style phrase that skews every render of one class. 11.39 mentions keeping visual-style instructions on a separate channel, but only in the direction of style altering STORY messages, not global style terms contaminating a per-subject visual prompt. The generalizable lesson (scope prompt-composition profiles per artifact class; global palette/ambience terms must never reach subject prompts; regression the profile-to-artifact mapping) has no home in the bible.

### 16. The forbidden-pattern sweep only diffs TRACKED files, so a new test file passes its own gate and then fails the commit
- **Source:** docs-handoffs
- **Ref:** docs/HANDOFF_LOG.md:2291-2293
- **Evidence:** "the B7 forbidden sweep only diffs TRACKED files, so a new test file passes its gate and fails the commit after -- that cost one red HEAD this session and is written down so it costs nobody else one."
- **Why no existing entry covers it:** No entry covers a gate whose input population is narrower than the population it gates. The words tracked/untracked appear nowhere in the 228 entries (verified by grep), and no entry discusses git visibility as the input selector for a static sweep. The nearest is 12.36, but it is the mirror-image defect (a traversal that is too INCLUSIVE, pulling in .venv) and its prescribed fix -- exclude directories from os.walk -- would not have prevented this and does not imply it. 12.35 (three-file contract) and 12.02 (15-step checklist) prescribe what to run, not what population the check must walk; 12.16 would have caught the red HEAD after the fact but teaches nothing about the gate's blind spot. The transferable lesson -- a pre-commit gate that samples git's index while the commit samples the working tree gives a green that proves nothing, so a sweep must walk the same set the commit will carry (tracked + untracked-not-ignored) -- is absent.

### 17. Concurrent agent windows share one git index: a docs commit swept another window's staged deletions and left origin RED with a registry naming five engines whose module was gone
- **Source:** docs-handoffs
- **Ref:** docs/HANDOFF_LOG.md:206-213; see also :408-409 and :462-465 (`git add -A docs/` swept another window's untracked file)
- **Evidence:** "**THE INCIDENT: commit `760a63ae` (banana window, 19:31) swept this rip's three staged `git rm` deletions into its own pushed docs commit**, leaving origin RED ~90 min -- a registry declaring five engines whose adapter module was gone (roster audit missing=4, `>= 30` floors red at 28). ... **Gotcha banked ... on a multi-window tree, stage NOTHING until your own commit moment -- the index is shared.**"
- **Why no existing entry covers it:** The bible has 12 git entries (12.09-12.18, 12.34) and none addresses concurrency between simultaneous sessions on one working tree. 12.15 is a stale .git/index.lock left by a crashed process (a lock, recovered by deleting it); 12.34 is sandbox permission failure on that same lock; 12.16 is post-push lockstep verification, which would DETECT the red origin but teaches nothing about its cause; 12.18's git rm guidance is about cascade failure when a listed file is already gone, not about another actor's staged content. Scans for multi-window/parallel/another session/two agents/pathspec return nothing (12.17 is handoff docs between SEQUENTIAL sessions, the opposite regime). The lesson -- the index is process-global shared state, so on a multi-window tree stage nothing until your own commit moment and commit by explicit pathspec, because `git add -A <dir>` or an early `git rm` hands your neighbor's half-finished work to whoever commits first -- has no home entry.

### 18. Test-ordering pollution from module reload: a test fails right after a sibling test file and passes 11/11 alone (class-identity breakage)
- **Source:** docs-handoffs
- **Ref:** docs/HANDOFF_LOG.md:1070-1073 and :928-929
- **Evidence:** "`test_public_domain_interpreter::test_empty_cast_is_rejected_and_retried_to_failure` fails when run right after `test_public_domain_sources` and passes 11/11 alone." and "the test pollution is reload-induced CLASS IDENTITY breakage"
- **Why no existing entry covers it:** I searched the whole file for reload, sys.modules, isinstance, conftest, fixture, monkeypatch, pollution, test order, in isolation, and identity, and no entry teaches in-process test isolation or reload-induced duplicate class objects. The nearest three all miss it: 06.05 is duplicate module copies ON DISK plus stale __pycache__ (fix: sync files, clear caches, restart) -- deployment hygiene, not a test-ordering verdict, and its remedy would neither prevent nor diagnose this; 12.36 is only about excluding .venv from os.walk during static scans; 12.01 is about extracting pure functions so pytest runs fast. 12.74 is the closest on test VALIDITY ('a green suite cannot distinguish correct-and-connected from correct-and-orphaned') but it is about reachability of the fixed function, not about a test whose verdict depends on which sibling file ran first. Nothing in the bible states the load-bearing rule here: a test that passes alone and fails in suite order is an isolation defect, and re-importing a module mid-suite mints a second class object so isinstance / except clauses against the first one silently stop matching. Genuinely new.

### 19. HTML block-tag stripping fused sentence boundaries in full_text, which is what most writer P0 deaths actually tripped over
- **Source:** docs-handoffs
- **Ref:** docs/HANDOFF_LOG.md:1580-1586, 1556
- **Evidence:** "`full_text` carries HTML block joins with NO separator (`PolygonsNASA/JPL-`, `ofEngine`, `.Let's`, `).The`), which is what 12 of the 15 P0 deaths actually tripped over" (the two remaining were "character-identical-except-`&nbsp;`")
- **Why no existing entry covers it:** 11.26 is the nearest neighbour and it is close - same area (llm-output), same P0 consequence (a verbatim-grounding gate kills the lane), same root family (the CORPUS's own text-assembly convention, not the model, is what breaks the match), and it even already prescribes the deterministic-pruning remedy the handoff says is the only thing that helps. But its defect and its remedy are the opposite polarity: 11.26's cause is 'a phrase that wraps across a newline (the corpus joins model fields with \n)' and its entire fix is 'Whitespace-normalize BOTH sides of the verbatim test (collapse every whitespace run to one space, lowercase)', with a static check that any verbatim-grounding file 'also carries a whitespace-collapse normalizer used on both sides'. That remedy is inert against claim 2 - when block tags are stripped with NO separator there is no whitespace to collapse, the words are genuinely fused ('PolygonsNASA/JPL-', 'ofEngine'), and a pack that fully complies with 11.26 still dies exactly this way. Nothing in the bible tells you to audit the payload BUILDER's block-boundary join before blaming the model, the prompt, or the validator. 08.08 carries the raw mechanic ('A concatenated filter duplicated a pixel-format token without its separator' -> 'Build filter tokens with explicit separators') but only as an ffmpeg filter_complex instance in phase 8, not as a text-ingest law. I also confirmed the bible has zero entries mentioning HTML, entity decoding, or &nbsp; anywhere, so the companion 'character-identical-except-&nbsp;' half is uncovered too. Promote it, and xref 11.26 in both directions so the two normalization polarities sit together.

### 20. getattr(ids, "shape", [len(ids)]) builds its default eagerly inside a bare except, so a fake tensor made the writer report generated_tokens = None
- **Source:** docs-handoffs
- **Ref:** docs/HANDOFF_LOG.md:1589-1592
- **Evidence:** "A fake tensor without `__len__` makes the writer report `generated_tokens = None`, because `getattr(ids, \"shape\", [len(ids)])` builds its default EAGERLY inside a bare `except Exception`. Three of my tests failed on that and the production code was innocent."
- **Why no existing entry covers it:** I looked hard for a match and the honest answer is no. 12.72 is the closest ('getattr(pack, seam, None) returns None for every valid mapped seam and masks a registry wiring defect as a missing-pack contract failure') and it is the only other getattr entry in the file, but it teaches a different mechanism and a different remedy: there the default is RETURNED and hides a defect, and the fix is 'resolve every declared seam through the canonical prompt_stages mapping and fail closed'. Claim 3's mechanism is that Python evaluates getattr's third argument BEFORE the call, so a defensive fallback is not lazy and can itself raise - a fix for 12.72 does not prevent it and vice versa. The swallowing half has only 05.07 as a neighbour ('Python raises NameError, surrounding try/except logs it and returns unchanged'), but 05.07's cause names the try/except in passing and its fix is purely 'change the reference to the correct constant name', tags [typo, extension, name-error, refactor, scope] - it never generalizes to 'a bare except around a defensive default converts a programming error into a plausible wrong value'. The third strand - a test double LESS capable than the real object producing three false failures while production code is innocent - has no entry at all; 12.74's 'no stub can satisfy it' is about static reachability assertions, the opposite concern. Three distinct uncovered laws in one small finding.

### 21. The beat partitioner solved an exact cover before a trimmed one, planning four clips where two were legal - plus a hang, a one-quantum trim cap, and a wrong SINGLE verdict
- **Source:** docs-handoffs
- **Ref:** docs/HANDOFF_LOG.md:2271-2278 and :4173-4178
- **Evidence:** "partition_beat ran TWO walks over the segment count (exact at every count, then trimmed at every count), so an exact cover at a HIGH count beat a trimmed cover at a LOW one. A 184-frame HuMo beat planned [85,33,33,33] because 184 is 0 mod 4 ... while [153,33] trim 2 was legal at count 2 all along." Earlier trio: "a tail-trim search capped at one quantum (832 coverable beats refused), an unmemoized recursion that HUNG rather than refused (18s at count=14 ...), and ... `join_mode_for` claiming SINGLE for targets no single render can cover (202 refusals in an 18k sweep)"
- **Why no existing entry covers it:** I searched every entry for search/planning vocabulary (partition, segment count, greedy, exhaustive, backtrack, memoize, recursion, quantum, combinatorial, cost function) and the bible has nothing on combinatorial planning correctness. The headline lesson -- a two-phase search that ranks by PHASE (exact cover, then trimmed cover) before ranking by COST (segment count) will return a legal but worse plan, and the two walks must be collapsed into one ordered walk over the real objective -- has no analogue anywhere. Nor does the unmemoized-recursion hang: 12.71 bounds an outer while-true campaign with a fail-closed ceiling and 11.13 bounds runaway token generation, but neither teaches that a recursive cover search must be memoized or bounded before shipping (18s at count=14 is exponential blowup, a different mechanism from an unterminated retry loop). The one-quantum tail-trim cap (832 coverable beats refused) is likewise an unrepresented search-window defect. Only the fourth sub-item -- join_mode_for claiming SINGLE for targets no single render can cover -- is partially covered by 12.73 (capability declaration overstates what the runtime does; planner commits work against it and the executor discovers the gap late). That partial overlap does not cover the dominant defect, so this earns an entry, ideally written as the planning/search class with 12.73 cross-referenced for the verdict-vs-runtime sub-case.

### 22. Image seed is derived from the prompt hash, so removing a text sanitizer silently moves every derived still; portrait hashes cascade to scene characters
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-08-05-shipped-qa\r4\driver_judgment.md section 5
- **Evidence:** removing the producer sanitizers does not merely miss the cache -- it **moves the seed**, which moves the image. And because `portrait_prompt_hash` feeds scene-character seeds, one portrait prompt with a slash in it shifts every scene still derived from it.
- **Why no existing entry covers it:** I looked hard for this and the bible does not carry it. The nearest neighbours each teach a different mechanism: 12.55 is about an identity that fails to CHANGE when it should (byte replacement at the same path reusing stale derived artifacts) and prescribes binding derived-cache keys to a full digest — it never contemplates the identity doubling as a generation input; 05.01 derives an offset from sha256(unique_input) and asserts 'Same seed produces same allocation across runs', but frames it as cross-instance coordination, not as a constraint on editing the input's normalizer; 12.43 requires a shared normaliser on both sides of a LOOKUP, not a normaliser feeding a seed; 12.51 ('assign one precise semantic purpose to each key') is the closest abstract framing but lives entirely in ledger/receipt provenance. The uncovered lesson has two load-bearing halves the bible never states: (1) when a cache key IS the RNG seed, changing the key-derivation does not merely miss the cache, it MOVES the artifact — so a cosmetic-looking text sanitizer is inside the determinism contract and cannot be deleted as dead code; (2) derived hashes chain (portrait_prompt_hash -> scene-character seeds), so one upstream prompt change has a blast radius far larger than the row that changed. Worth a phase-06 or phase-12 entry whose verify is: change the sanitizer, assert every derived seed and every downstream still is unchanged.

### 23. IS_CHANGED returning time.time() on the writer and freeze cascade makes a deterministic replay leg impossible
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-07-12-dynamic-story-visual\r2\judgment.md row 12
- **Evidence:** **The live replay leg cannot pass**: writer + cascade `IS_CHANGED` return `time.time()`, so a requeue writes a FRESH story ... `IS_CHANGED` -> `return _t.time()` with the comment "always re-execute"
- **Why no existing entry covers it:** Not covered, and the bible currently teaches the opposite. 06.01 PRESCRIBES the exact anti-pattern as its fix — 'Implement IS_CHANGED returning time.time() (always refresh)' — with only a cost caveat ('Don't add to compute-heavy nodes that don't need it') and a verify that celebrates re-execution ('Re-queue a workflow; data-fetch node runs again'). 06.02's 'Treat IS_CHANGED as a perf hint, not a contract' points the other way (do not rely on it to skip work) and is scoped to the None-input crash. 06.03 concerns ComfyUI's own signature builder, not a node's returned signature. Nothing anywhere states the consequence this claim found: an always-refresh IS_CHANGED forfeits replayability outright, and because IS_CHANGED propagates to dependents, one writer plus a freeze cascade makes a deterministic replay leg unachievable for the whole subgraph — so a QA plan that promises a replay leg is promising something the wiring forbids. An entry recommending X cannot be counted as teaching X's failure class. Best shape: a new phase-06 entry that amends 06.01 with the trade-off (content-derived signature vs always-refresh) and adds an explicit cross-reference, so 06.01 stops reading as unqualified advice.

### 24. Windows portability in probe/encode tooling: EGL context path attempted on Windows, and a POSIX null path used as an ffmpeg sink
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-07-08-scope-accel\r3\final.md
- **Evidence:** On Windows, the ModernGL probe should use the platform default/WGL path. EGL should be skipped ... Windows null-sink ffmpeg should use the null muxer with `NUL`, not a Unix path.
- **Why no existing entry covers it:** I searched the whole file for EGL, WGL, OpenGL, ModernGL, NUL, /dev/null, sys.platform, os.name, platform.system, POSIX and cross-platform. The GPU-context half has zero coverage: there is no entry anywhere about selecting a graphics/windowing backend per platform. The null-sink half is likewise absent — the only DEVNULL mention is 09.02, which prescribes subprocess stdout=DEVNULL to cure a pipe deadlock and says nothing about an ffmpeg output sink or a hardcoded POSIX device path. The scraper's closest, 12.27, is Windows-vs-POSIX SLASH DIRECTION inside huggingface_hub symlink targets — same continent, different failure: it teaches that a path convention must match the OS that created a symlink, not that a Linux-default API path or device constant will be attempted unconditionally on a Windows-only target. 02.03 (no FA2 wheel on this platform, log a platform-accurate message and stop chasing it) is the nearest posture match but is an install-time dependency lesson, not a runtime backend-selection one. On a stack whose CLAUDE.md pins the whole project to Windows, 'probe/encode tooling carries Linux defaults that must branch on the platform, and a probe that silently falls back is worse than one that refuses' is a real recurring class the bible does not yet hold.

### 25. Ordered string-replacement table is not closed under its own outputs: a later row re-matches an earlier row's replacement in ONE pass
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-08-06-banana-route\r1\judgment.md (THE SECOND FINDING)
- **Evidence:** `("hand grenade", "banana bomb")` at `:137` ... `("bomb", "banana bomb")` at `:221` re-matches the output of the first. Result: **`hand grenade` -> `banana banana bomb` in ONE application.**
- **Why no existing entry covers it:** No entry teaches closure of a rewrite table under its own outputs. I searched idempot*, closed under, cascade, re-apply, second pass, self-match, own output, single-pass, str.replace, rewrite/lookup/mapping table -- nothing. The nearest candidates all fail on mechanism: 10.02 (scraper's pick) is about sentinel VARIETY in content-filter substitutions, not about one substitution feeding another; 12.43 teaches applying a shared normaliser to BOTH sides of a lookup (key namespacing, not sequential rewrite); 11.41 is about two normalizers each refusing to run until the other's defect is gone (mutual blocking, the opposite failure -- nothing runs, rather than one running on another's output); 12.37 is capture typing. The specific, actionable lesson -- an ordered substitution table must be closed under its own outputs, or applied against the original text with a single non-overlapping sweep, because 'hand grenade' -> 'banana bomb' -> 'banana banana bomb' happens in ONE application -- is absent. Worth its own entry.

### 26. Path-detection guard rejects os.altsep, which on Windows is "/", so ordinary prose ("a black/white striped scarf") suppressed the still for that beat
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-08-05-bug-sprint\r1_input.md item G
- **Evidence:** `path_guard_arm` rejects `os.altsep`, and on Windows `os.altsep` IS `/`, so a character described as wearing "a black/white striped scarf" tripped the guard and the dispatcher recorded skip evidence and rendered NO still for that beat.
- **Why no existing entry covers it:** Grep for altsep returns nothing; the only slash entry is 12.27, which is about symlink TARGET slash direction in the HF hub cache -- a resolution failure on a real filesystem path, not a discriminator misfiring on prose. The generic half of this lesson is covered several times over (11.08 legitimate text matching a structural detector's surface shape; 12.61 legitimate content classified as forbidden; 12.37 a discriminator that cannot tell sentinel from domain value; 05.06 and 11.56 for the 'guard trips and the artifact silently does not exist' consequence). What is nowhere in the bible is the concrete, high-value trap: os.altsep IS "/" on Windows, so a path-rejection guard assembled from os.sep + os.altsep rejects any prose containing a slash, and a single character in a costume description silently costs you a render. The bible carries entries at exactly this specificity for Windows path traps (12.26 dirname miscount, 12.27 slash direction, 02.15 cp1252), so this fits its granularity and is not currently taught. Two-part entry: never build an 'is this a path?' test out of characters legal in content, and a guard rejection must fail observably rather than yield a missing artifact.

### 27. Forbidden-identifier gate builds its input from git diff, so untracked new modules are invisible to it -- green, then red on first commit
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-08-05-bug-sprint\r1_input.md item J
- **Evidence:** `tests/test_b7_forbidden_sweep.py` builds its input from `git diff s29-clean-slate-gate -- *.py`, which covers TRACKED files only. A new module or test file was invisible to the gate for exactly as long as it stayed untracked: green, then red on its first commit with nothing else changed.
- **Why no existing entry covers it:** Grep for untracked, tracked file, changed files, incremental, baseline branch returns nothing; git diff appears only in 12.14 (CRLF phantom diff) and 12.16 (lockstep push verification), neither about gate scope. 12.36 (scraper's pick) is the nearest and is genuinely adjacent -- it is the only entry about which files a static sweep collects -- but it is the INVERSE failure: it teaches excluding .venv to stop false POSITIVES, and gives no guidance about VCS-derived deltas or false NEGATIVES. 12.74 shares the false-green tag but its mechanism is an unreachable function, not an unreachable file. 05.10's symptom mentions a node 'the unit-test suite never actually exercises' but the entry is about UnboundLocalError from a shadowed import. The distinct lesson -- a gate's input set must be the shipped source tree, not a VCS delta, because such a gate is green for exactly as long as the offending code stays untracked and then fires on an unrelated commit, pointing the investigation at the wrong change -- is uncovered and is a strong pairing with 12.74 under the false-green family.

### 28. .gitignore pattern /tmp_* misses the tmp/ directory by one character -- 6,308 untracked files / 272 MB in every git status
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-08-05-bug-sprint\r1_input.md (Repo hygiene)
- **Evidence:** `.gitignore:263` had `/tmp_*`, which matches the 93 MB `tmp_audit_results.json` at the root and does NOT match the `tmp/` DIRECTORY beside it. Result: **6,308 untracked files / 272 MB** reported by every `git status`.
- **Why no existing entry covers it:** No entry covers ignore/exclusion-pattern precision or verifying that an exclusion actually excludes. The three nearest are all misses: 12.18 (area: hygiene) is about dev docs ALREADY COMMITTED to a public tree, cause 'dev docs committed to public tree', fix 'git rm one file at a time' -- a different cause and a different fix from an ignore rule that never fired; 12.36 is the closest structural analogue (exclusion lists must actually cover the real directories) but is scoped to os.walk/glob in the test harness and to .venv/venv false positives, not to git ignore semantics; 04.11 teaches the general 'the string never matches, the branch is dead, silently never fires, no exception, no warning' shape but its fix (typed enum constants imported at every call site) does not transfer to a .gitignore glob. Nothing in the file teaches that a leading / anchors, that tmp_* is a prefix and not a directory, that a directory needs a trailing slash, or that `git check-ignore` should be run to prove the rule fires. This matters for the bible's own protocols: 12.15 verifies with 'git status runs cleanly' and 12.16 relies on lockstep git inspection, both of which are defeated by 6,308 untracked entries. The claim is verified real -- C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\.gitignore now carries the fix at lines 263-270 with a comment naming the miss, and the same file shows two earlier near-misses of the same class at lines 120-121 and 186-189, so it recurs. Recommend a new phase-12 hygiene entry.

### 29. Un-escaped prompt content in log lines allows log-record forgery via embedded newlines
- **Source:** qa-kibitz
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\kibitz-runs\2026-08-04-d1-still-unmaterialized\r2\judgment.md row MF2
- **Evidence:** repr-escape so newlines cannot forge log records | CONFIRMED. Log-record forgery via prompt content is a real concern I missed
- **Why no existing entry covers it:** I could not find any entry that teaches writer-side escaping of free-form content before it enters a line-delimited evidence stream. Grepped the full file for escap*, repr(, str(exc), newline, inject, sanitiz, untrusted, forge, spoof, log-record -- nothing lands. The scraper's 10.01 is content-safety filtering of scraped text going INTO an LLM, a different direction and a different threat. The nearest true neighbour is 12.37 (a content token FINAL matching the untyped capture in '=== SCENE (\S+?) ==='), which is the same abstract shape -- content indistinguishable from structure -- but its entire remedy is on the READING side ('type the capture group to the actual domain'), and it is scoped to a script parser, not to logs. The bible's several log entries (11.05 clarity, 12.40 name-the-field, 05.09 unconditional outcome line) all treat the log as trustworthy by construction and only argue about what to put in it. The missing law -- the evidence stream is itself forgeable by the content it carries, so repr-escape before writing -- is not stated anywhere. This one is genuinely new.

### 30. PowerShell-launched CLI run: a semicolon inside an episode title split the command line, so argparse rejected the tail as unrecognized arguments
- **Source:** loose-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\run-20260723-003628-span-lla28.err.log (whole file). The mangled title survives into the successful sibling run as episode_title='SPAN_TE_PI;LLA28' in task-20260723-003614-soak.log.
- **Evidence:** otr_canonical_api_run.py: error: unrecognized arguments: TE PI;LLA28 short, atmospheric prompt for a calm test render.
- **Why no existing entry covers it:** No entry teaches shell-argument TOKENIZATION. I searched every PowerShell/CLI/launcher entry and every escaping-adjacent tag: 02.11, 02.12, 02.13 and 02.15 are all ENCODING (BOM, mojibake, cp1252/UnicodeEncodeError on a detached boot) -- a different failure mode from an argument being split; 12.13 only requires a handed block to begin with an absolute cd; 12.09/12.10/12.11/12.34 are git hangs, stderr false alarms and index.lock; 12.47/12.52 are file-based env-hook leaks. Nothing anywhere in the 228 entries says that a free-text value (LLM-authored title, operator string) placed on a PowerShell/cmd command line must be quoted, stop-parsed (--%), or passed via a file/stdin/env because ';', '&', '|', '$' and backtick are statement separators, and nothing covers the second half of the evidence -- that the mangled fragment then PERSISTS as canonical state ('SPAN_TE_PI;LLA28' surviving into the sibling run's episode_title), so a shell-tokenization accident becomes durable data corruption. The project CLAUDE.md carries an ASCII-safe-quoting rule for commit messages, but that is operator instruction, not a bible entry, and it addresses smart quotes/em-dashes rather than metacharacter splitting. Genuine gap.

### 31. ComfyUI-OldTimeRadio loaded twice in one ComfyUI process from two custom_nodes roots — prestartup runs twice, all 34 nodes register twice, and the pack's HTTP routes are registered twice
- **Source:** loose-logs
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio\task-cricket-server-visible.log lines 58-61 (prestartup x2), 131-133 and 172-174 (node + route registration x2), 201-202 (both import paths). Same double-load in task-20260723-003614-soak.log and task-cricket-server-20260723-004236.log.
- **Evidence:** [OldTimeRadio] OK - All 34 nodes loaded successfully
[OldTimeRadio] HTTP route registered: GET /otr/latest_ledger (with CORS)
[OldTimeRadio] HTTP routes registered: POST /otr/video_render_single, POST /otr/video_render_soak
... (identical block repeats) ...
[INFO]    0.7 seconds: C:\Users\jeffr\ComfyUI-Installs\ComfyUI\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio
[INFO]    1.0 seconds: C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio
- **Why no existing entry covers it:** Verified in evidence: one process (single '### ComfyUI Revision: 5607' banner, one User directory line) loads the pack from BOTH C:\Users\jeffr\ComfyUI-Installs\ComfyUI\ComfyUI\custom_nodes and C:\Users\jeffr\Documents\ComfyUI\custom_nodes; the prestartup HF_HOME/safetensors-mock block, the '34 nodes loaded' line, and the aiohttp route lines each appear twice. Searched hard and found no covering entry. Nearest neighbours fail on the lesson: 06.05 (sync) names 'two copies of code (source repo + custom_nodes)' but its failure mode is stale edits, and its FIX actively prescribes maintaining two synced copies — which is the very setup that causes this bug when both copies sit under scanned roots; it says nothing about double registration. 03.03 (naming) covers NODE_CLASS_MAPPINGS key collisions between DIFFERENT packs where one silently wins, and its remedy (namespace your keys) is useless against the same pack twice. 12.23 (registration) is about a stale __init__.py registration list, not a duplicated scan root. 01.01/01.04 cover finding the right ComfyUI install, not two roots being scanned by one process. Grep for PromptServer/routes/http route/sys.path/module-name collision returns only 08.05's send_sync fix — the bible has ZERO coverage of pack-level HTTP route registration, so 'prestartup side effects and route registration are not idempotent under a double load' is entirely new. Uncovered lesson: a pack can be loaded twice in one process, and every module-scope side effect (env mutation, monkeypatch, aiohttp route, background thread) then runs twice, with the second copy's classes silently winning the registry.

### 32. INPUT_TYPES advertises two sockets the node's FUNCTION cannot accept (TypeError at execution)
- **Source:** git-fixes
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio @ b00b047f (docs/2026-07-31-NEWBUG-scene-sequencer-music-sockets.md)
- **Evidence:** SceneSequencer.INPUT_TYPES declares music_cue_audio and music_cue_manifest_json, but sequence() accepts neither and has no **kwargs, so wiring either socket raises TypeError at execution.
- **Why no existing entry covers it:** No entry ties INPUT_TYPES declarations to the FUNCTION method's actual parameter list. Phase 04 owns INPUT_TYPES and has 13 entries -- 04.01/04.02 (positional widgets_values drift), 04.03 (hidden unique_id), 04.04 (VALIDATE_INPUTS None), 04.05 (lazy), 04.06 (stuck widget defaults), 04.07-04.13 (workflow-to-API slot mapping, converted widgets, socket-only custom types, dropdown string drift) -- and every one of them is about VALUE mapping, never about whether the declared input NAME exists in the function signature. The regression checklist in 12.02 step 8 audits 'INPUT_TYPES vs widgets_values', not INPUT_TYPES vs FUNCTION params. Nearest neighbours all fail on inspection: 12.73 is declaration-vs-runtime but strictly about numeric ranges/menus and env overrides, and its verify procedure (feed declared min/max through the execution resolver) would pass cleanly on a node whose signature is missing the parameter entirely; 12.23 is the mirror-image at registration level (NODE_CLASS_MAPPINGS naming a class that no longer exists), not at input level; 09.06 is a caller sending the wrong SHAPE (string vs dict) to somebody else's cloud node, not a node's own advertised socket having no landing site. The teachable rule -- every key in INPUT_TYPES required/optional must appear in the FUNCTION signature or be absorbed by **kwargs, statically auditable by AST -- is absent, and it is a first-order ComfyUI failure (wiring the socket raises TypeError at execution, invisible until someone wires it).

### 33. importlib.reload in one test replaced class objects, breaking another test's except clauses -- a failure that only appeared when the two files ran adjacently
- **Source:** git-fixes
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio @ 1a746c25 (tests/test_public_domain_sources.py)
- **Evidence:** Reload REPLACES class objects, and test_public_domain_interpreter.py imports its exception classes at COLLECTION time -- so once this test had run, that file's except clauses no longer matched instances raised by the reloaded module ... It passed alone, full-suite ordering hid it
- **Why no existing entry covers it:** I could not find an entry that teaches this. The bible mentions importlib only as an isolation technique (03.01, 'isolated per-node loading via importlib.import_module wrapped in try/except') and reload only in the opposite direction (06.05, 're-queue does NOT reload modules'). Nothing anywhere covers the actual mechanic: reload REBINDS the module's names to NEW class objects, so any symbol captured earlier -- except clauses, isinstance checks, registry keys, sentinel classes -- silently stops matching, and the breakage is invisible until a particular file ordering puts the reloader before the capturer. The nearest analogies fail on the lesson: 12.47/12.52 are file/env-var leaks with a consume-once remedy that does not transfer; 12.43 covers silent lookup mismatch but between two STRING forms of a key, with a shared-normalizer fix; 12.55 covers path-vs-byte identity for durable assets. None of them would stop an author from calling importlib.reload inside a pytest module. The remedy (do not reload in-process, or re-import the symbols after reload, or run reload-based tests in a subprocess) plus the diagnostic (a test that passes alone and fails only adjacent to a sibling is a module-state contamination, not a flake) is new teaching.

### 34. A scripted refactor rewrote a module's __all__ into literal 'self.'-prefixed strings, and the whole suite was blind to it
- **Source:** git-fixes
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio @ 80935627 (nodes/_otr_video_engines/eng_wan_ti2v.py)
- **Evidence:** the recipe-seam refactor's scripted replacement rewrote eng_wan_ti2v's __all__ into ["WanTi2vEngine", "self.recipe_id", ...] -- literal self.-prefixed strings naming nothing. Invisible to the suite because __all__ only affects star-imports.
- **Why no existing entry covers it:** Genuine gap. Zero lexical hits anywhere in BUG_BIBLE.yaml for `__all__`, star-import, scripted/bulk/mechanical replacement, or export-list validation. Nearest neighbors are real but do not teach this lesson. 12.23 (Ghost Node Registration) is the closest: 'refactoring node classes without cleaning up the NODE_CLASS_MAPPINGS or _NODE_MODULES lists' -- but its whole teaching rests on a LOUD boot signal (verify: "All N nodes loaded successfully appears with 0 skips"), which is precisely the signal `__all__` can never produce. 12.74 supplies the adjacent concept (zero-reader constructs, false-green suite, 'assert STATICALLY over the shipped source rather than a booted object') but scopes it to a fix landing in an unreachable FUNCTION -- reachability of callable code, not integrity of a declaration nothing calls. 12.02 step 6 ('grep for dead references') is a generic net that would only catch this if someone thought to point it at a string list. The uncovered lesson is the mechanism: a scripted string replacement can produce syntactically valid, AST-parseable garbage inside a zero-consumer declaration, so no import error, no boot skip, no test failure, and no coverage tool can see it -- which means every zero-reader declaration needs an explicit static assertion that each name it exports resolves in its own module. Nothing in the bible says that.

### 35. A per-key short-work fallback made sibling beats disagree, so an adapted scene quoted one identical mid-passage on every beat and arcs could run backwards
- **Source:** git-fixes
- **Ref:** C:\Users\jeffr\Documents\ComfyUI\custom_nodes\ComfyUI-OldTimeRadio @ cdf8bffb (beat-keyed source window module)
- **Evidence:** The short-work fallback was decided PER KEY. Integer division makes bucket sizes differ by one character, so for bodies where length over beats lands near the floor SOME beats kept their own region while their siblings collapsed to the whole body -- unsorted, duplicated starts, and an arc that could run backwards
- **Why no existing entry covers it:** No entry teaches this class. The lesson is: when a set of derived slices is computed key-by-key, a DEGRADATION/FALLBACK threshold must be decided ONCE for the whole family, because integer-division bucket sizes differ by one character and a per-item threshold evaluated near that floor flips inconsistently -- some siblings keep their own region while others collapse to the whole body, breaking the family's collective invariants (disjointness, sorted starts, monotonic progression). I checked every plausible neighbour and each fails on mechanism: 12.45 is the closest on outcome (a derived-window heuristic producing degenerate coverage) but its cause and fix are about a fixed-interval formula that does not SCALE with content ('prefer one that scales WITH the available data') -- here the formula does scale, the fallback decision is what diverges. 12.69 requires stable sorting and next-start ownership so spans are not duplicated, but its cause is summing full render requests instead of honouring positioned ownership, a media-timeline defect with no threshold or per-key element. 12.41, 12.42, 12.67 and 12.71 all teach 'one shared owner so divergence is impossible', but every one of them is about divergence across CODE SITES (two regexes, two write paths, several word counters, several producer families), never divergence across DATA ITEMS produced by a single code path. 12.58 ends with 'siblings sharing a parent never collapse', which matches the symptom, but its cause is an identity-namespace collision between line id and beat id, not a threshold. 10.04 ('effective size = N per filter, not N total') is the nearest thing to a boundary-arithmetic lesson and is about pool sizing, not slice derivation. Nothing in the bible mentions integer-division/off-by-one bucket boundaries, per-item vs collective fallback, or a required monotonicity/ordering invariant over a keyed family of source windows. Genuinely new.

## Covered, but the verifier flagged a bible refinement

### Engine's `roles` tuple omitted a role its `required_inputs` actually allow — director fail-closed a compatible operator pick at execute  ->  covered by 12.73
12.73 (capability-declaration-vs-runtime) is this exact class: a hand-written static declaration diverges from what the runtime actually honours, a planner/selector commits against the declaration, and the disagreement surfaces as a late fail-closed at the executing step. Its fix is verbatim the BUG-401 lesson -- 'Make the declaration describe what the runtime DOES, not what the component is believed capable of' -- and its verify already says 'for discrete menus test every member, not the endpoints, since inherited min/max are not menu members', which is precisely the per-role sweep (`engine_fits_role` over all five roles) that BUG-401 added. 11.27 is a second match: a hand-curated static table missing a legitimate user-selectable entry, so a valid pick hard-fails downstream. RESIDUAL NUANCE worth noting to the maintainer rather than a new entry: 12.73's cause channels only name the OVER-declaration direction (declaration overstates the component -> false accept). BUG-401 is the mirror -- the declaration UNDER-states, so a capable option is falsely REFUSED -- and 12.73's verify (feed each declared value through the resolver, require it back unchanged) is structurally incapable of catching under-declaration. If this earns anything it is a one-clause extension to 12.73's cause/verify (assert the inverse: for every menu member the resolver accepts, the declaration must list it), not a 229th entry.

### Every non-face video engine inherited the talking-head 480x832 PORTRAIT canvas default, so stills/b-roll rendered pillarboxed inside the 16:9 deliverable  ->  covered by 07.23
07.23 is the same structural defect one field over. Its cause -- 'capability was checked only at the terminal dispatcher; upstream planning ... [did not] resolv[e] each role's effective engine ... and its declared init-image capability' -- and its fix -- 'Centralize a per-role effective-engine ... decision' -- is exactly BUG-407's repair: stop keying the canvas off a hardcoded two-engine allowlist (`ltx_video`/`wan_i2v`) and derive it from the engine's declared family, with only `audio_driven_face`/`lipsync_overlay`/`character_3d` keeping portrait. 05.08 is a strong second: 'Decouple gates from role policy ... Audit every feature flag's gate condition to confirm it matches the actual dependency, not a sibling feature's name' -- BUG-407 is the same inverted-polarity mistake, where one family's REQUIREMENT was installed as the global default and the general case was allowlisted by engine name. Notably the same bug log's BUG-403 FIX1 is the identical `_SCENE_INIT_FAMILIES` allowlist miss on the same function, which is direct evidence this is a recurring instance of an already-taught class rather than a new one. GAP THAT IS REAL BUT NARROW: the bible has no domain vocabulary for canvas/aspect at all (grep for canvas|aspect|portrait|landscape|16:9 returns only 07.11's composite-blend pillarbox and one incidental mention in 12.73), so a reader searching those words will not land on 07.23. That argues for a tag/xref on 07.23, not a new entry.

### Failed llama-quantize left a plausible-looking 0.01 GB .gguf at the canonical output path and a downstream log recorded that path as the Q4_K_M model  ->  covered by 12.50
12.50 (test-orchestration / exit-code / durable-output) already teaches exactly this class: it defines an ordered terminal-evidence contract in which 'a real nonzero child exit or missing canonical success remains failure' and success may only be declared when the authoritative runner result AND the required durable output checks both pass. Here QUANT_EXIT=1 was captured in the same log yet the artifact was recorded as the model — a straight violation of 12.50's contract, just in the false-positive direction rather than 12.50's false-negative direction. 05.06 (completion-check, tags false-success/artifact-check) reinforces it: never let a missing/ambiguous status default to success, and verify artifacts after the status check. Residual nuance not spelled out anywhere — that artifact EXISTENCE is not artifact VALIDITY, so a 0.01 GB stub passes an existence check — is a refinement of 05.06/12.50, not a new lesson; 12.02 step 15 and 12.16 already teach 0-byte/size scanning as a validity gate in the git lane. Not truly uncovered.
